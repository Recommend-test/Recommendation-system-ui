import { CompositeEntityActionConfig, EntityActionTypes } from "../../models";
export interface CreateEntityActionTypesPayload {
    readonly entityType: string;
    readonly storeFeature?: string;
}
/**
 * Creates the action types that a reducer created via createEntityReducer
 * reacts to.
 * @param payload
 * @param config
 */
export declare function createEntityActionTypes(payload: CreateEntityActionTypesPayload, config?: CompositeEntityActionConfig): EntityActionTypes;
