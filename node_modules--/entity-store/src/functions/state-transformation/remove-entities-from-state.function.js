"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
function removeEntitiesFromState(state, payload) {
    var ids = state.ids.filter(function (id) { return !payload.some(function (x) { return x === id; }); }), entities = {};
    ids.forEach(function (id) {
        entities[id] = state.entities[id];
    });
    var selectedIds = state.selectedIds.filter(function (x) { return payload.some(function (y) { return y === x; }); });
    return __assign(__assign({}, state), { ids: ids, entities: entities, selectedIds: selectedIds });
}
exports.removeEntitiesFromState = removeEntitiesFromState;
