"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var util_1 = require("util");
/**
 * Returns the first selected entity
 * in a given entity state or null if
 * there is none.
 *
 * Useful when using the entity
 * store for selection models.
 *
 * @param {EntityState<T>} state
 * @returns {T}
 */
function getFirstSelected(state) {
    var firstSelected = state.selectedIds.map(function (id) { return state.entities[id]; })[0];
    if (util_1.isNullOrUndefined(firstSelected))
        return null;
    return firstSelected;
}
exports.getFirstSelected = getFirstSelected;
