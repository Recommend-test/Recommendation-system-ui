import { EntityState } from "../../models";
/**
 * Returns all entities in a given entity state
 * as array.
 *
 * @param {EntityState<T>} state
 * @returns {T[]}
 */
export declare function getAll<T>(state: EntityState<T>): T[];
