"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var models_1 = require("../models");
var functions_1 = require("../functions");
/**
 * Performs add, update, remove, clear, and select operations in all target reducers
 *
 * @example
 * import { CompositeEntityAction } from "entity-store";
 *
 * // Action that adds user Jason to store and updates
 * // the location01's label
 * const action = new CompositeEntityAction({
 *     add: [{
 *         entityType: "User",
 *         payload: {
 *              user01: {
 *                  id: "user01",
 *                  label: "Jason"
 *              }
 *         }
 *     }],
 *     update: [{
 *         entityType: "Location",
 *         storeFeature: "LocationManager",
 *         payload: {
 *              location01: {
 *                  label: "Jason's new home"
 *              }
 *         }
 *     }]
 * });
 */
var CompositeEntityAction = /** @class */ (function () {
    /**
     * A collection of entity-action payloads
     */
    function CompositeEntityAction(payload, config) {
        if (config === void 0) { config = models_1.defaultCompositeEntityActionConfig; }
        this.payload = payload;
        this.config = config;
        this.type = functions_1.createCompositeEntityActionType(this.payload, config);
    }
    return CompositeEntityAction;
}());
exports.CompositeEntityAction = CompositeEntityAction;
